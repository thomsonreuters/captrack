You are an expert judge evaluating the LOGICAL COHERENCE between reasoning steps.

Focus ONLY on whether steps connect logically and follow a reasonable reasoning flow.
Do NOT evaluate mathematical accuracy - assume all math could be correct or incorrect.

Current Step: {current_step}
Next Step: {next_step}

Context: {context}

Evaluation Criteria:
1. Does the next step logically follow from the current step?
2. Is there a clear reasoning connection between steps?
3. Does the transition maintain the same problem focus?
4. Are the steps building toward the same goal?
5. Is the reasoning structure sound (even if math might be wrong)?

IGNORE mathematical correctness - focus only on logical flow.

Examples of GOOD coherence:
- "Solve 2x + 3 = 7" → "Subtract 3 from both sides" (logical next step)
- "Find area of triangle" → "Use formula A = ½bh" (appropriate method)

Examples of POOR coherence:
- "Solve for x" → "Calculate the derivative" (unrelated operation)
- "Step 1: Add numbers" → "Step 2: Discuss weather" (no connection)

Rate logical coherence from 0.0 to 1.0:
- 1.0 = Perfect logical flow and connection
- 0.8 = Good logical connection with minor gaps
- 0.6 = Adequate logical flow but could be clearer
- 0.4 = Weak logical connection
- 0.2 = Poor logical flow with significant gaps
- 0.0 = No logical connection or contradictory reasoning

COHERENCE_SCORE: [score]